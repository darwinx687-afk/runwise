# 发布分享素材包

这些是 Runwise `v0.1.0-preview.0` 的草稿分享素材。请在维护者确认渠道、发布时间和措辞之前，不要对外发布。

## GitHub 中文介绍

Runwise `v0.1.0-preview.0` 已作为公开预览版本发布。

Runwise 是一个本地优先的开源工具，用于 AI Agent 就绪度检查、trace 验证、静态 replay、Failure-to-Eval 生成和生态兼容性检测。当前版本仅支持源码安装：暂未发布 npm package，暂未上架 GitHub Marketplace，不包含 cloud、hosted SaaS 或官方集成声明。

Release: https://github.com/darwinx687-afk/runwise/releases/tag/v0.1.0-preview.0

欢迎反馈 AI Agent 就绪度、trace replay、Failure-to-Eval 和国内大模型服务商检测相关问题。

## 即刻 / 小红书技术向短文

Runwise 的第一个公开预览版 `v0.1.0-preview.0` 已发布。

它是一个本地优先的 AgentOps 工具，面向 AI Agent、MCP server、RAG 和 LLM application，提供就绪度检查、trace 验证、静态 replay、Failure-to-Eval 和生态兼容性检测。

当前只支持源码安装，没有 npm 发布，没有 cloud，没有 hosted SaaS，也没有官方集成或合作声明。

Release: https://github.com/darwinx687-afk/runwise/releases/tag/v0.1.0-preview.0

## V2EX / 掘金发布稿

我在做一个本地优先的 AI Agent readiness / trace replay / eval 工具，叫 Runwise。第一个 public preview `v0.1.0-preview.0` 已经发布。

目前可以做这些事：

- `runwise doctor` 生成本地就绪度报告
- 输出 JSON、Markdown 和静态 HTML 报告
- 验证本地 `runwise.agent_trace` 文件
- 从 trace 生成静态 replay 报告
- 从失败 trace 生成 Failure-to-Eval 草稿
- 检测 MCP、RAG、browser agent、OpenAI-compatible API 和国内大模型服务商等本地兼容性信号

当前版本只支持源码安装。npm、Marketplace、hosted SaaS、cloud sync、官方集成声明都没有包含。

希望收到关于 AI Agent 就绪度规则、trace replay 可读性、Failure-to-Eval 输出结构和国内大模型检测信号的反馈。

Release: https://github.com/darwinx687-afk/runwise/releases/tag/v0.1.0-preview.0

## 朋友圈 / 社群简短介绍

Runwise 第一个公开预览版发布了：一个本地优先的 AI Agent readiness、trace replay 和 Failure-to-Eval 工具。

当前只支持源码安装，没有 npm、没有云服务、没有 SaaS，也不做官方集成声明。欢迎帮忙看一看报告、trace replay 和 eval 草稿是否对 AI Agent 落地有帮助。

Release: https://github.com/darwinx687-afk/runwise/releases/tag/v0.1.0-preview.0

## 面向国内企业 AI 落地的介绍

Runwise 面向正在落地 AI Agent、MCP server、RAG 和大语言模型应用的团队，目标是在 demo、CI 门禁和发布审查前生成可审阅的本地证据。

第一个公开预览版包含本地就绪度检查、报告生成、trace 验证、静态 replay、Failure-to-Eval 生成和国内大模型服务商兼容性信号检测。

当前版本不包含 cloud、hosted SaaS、数据库、登录、计费、模型调用或官方生态合作声明。所有输出都以本地、可复现、可审阅的文件为主。

Release: https://github.com/darwinx687-afk/runwise/releases/tag/v0.1.0-preview.0

## 希望大家反馈什么

- AI Agent 就绪度规则还缺哪些？
- trace replay 报告是否方便复盘失败或高风险运行？
- Failure-to-Eval 产物是否适合作为评测用例草稿？
- MCP、RAG、browser agent 和 OpenAI-compatible API 的检测是否清晰？
- 国内大模型服务商信号是否有用，命名是否谨慎？
- 下一轮需要补哪些文档或示例？
