{
  "name": "@runwise/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Runwise command-line interface for local readiness, trace, replay, and eval workflows.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "runwise": "./src/index.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@runwise/dashboard": "workspace:*",
    "@runwise/core": "workspace:*",
    "@runwise/reporter": "workspace:*",
    "@runwise/schemas": "workspace:*"
  },
  "scripts": {
    "dev": "tsx src/index.ts doctor",
    "check": "tsc --noEmit -p tsconfig.json"
  }
}
