{
  "name": "@runwise/core",
  "version": "0.0.0",
  "private": true,
  "description": "Core local scanner, rule engine, trace validation, replay, and eval generation logic for Runwise.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@runwise/integrations": "workspace:*",
    "@runwise/schemas": "workspace:*"
  },
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json"
  }
}
