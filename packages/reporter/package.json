{
  "name": "@runwise/reporter",
  "version": "0.0.0",
  "private": true,
  "description": "Runwise JSON, Markdown, YAML, and static HTML artifact rendering.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@runwise/schemas": "workspace:*"
  },
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json"
  }
}
