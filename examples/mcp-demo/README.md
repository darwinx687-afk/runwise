# MCP Demo

This lightweight example exists for Runwise ecosystem detection. It shows local MCP signals without running an MCP server or tool.

Use Runwise against this folder to review MCP readiness, approval policy expectations, trace coverage, replay artifacts, and eval case generation.
