# China-Ready LLM Demo

This lightweight example shows local configuration signals for OpenAI-compatible APIs and China-ready LLM providers.

It contains placeholder environment variable names only. Do not put real API keys in this example.
